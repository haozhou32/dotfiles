# Formatting Rules

Code formatting conventions:

- Use 2-space indentation unless the project's established standard differs
- Remove trailing whitespace
- End files with a single newline
- Limit lines to 100 characters for readability
- Follow the project's existing formatter configuration (Prettier, ruff, gofmt, etc.)
